[package]
name = "apriori-mev-dto"
version = "0.1.0"
edition = "2021"
description = "Protocol definitions and DTOs for APriori authentication and communication"

[dependencies]
# Serialization
serde = { version = "1", features = ["derive"] }
num_enum = "0.7"


